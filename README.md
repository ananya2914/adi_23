ADI's Birthday Countdown

Files:
- `index.html` — root page (Vite entry file)
- `src/main.js` — app initialization and countdown logic
- `src/confetti.js` — dynamic, continuous confetti engine
- `src/styles.css` — theme variables (light & dark), layout and animations

Quickstart (Vite)
1. Install dependencies:
   ```
   npm install
   ```
2. Start dev server:
   ```
   npm run dev
   ```
3. Open the URL printed by Vite (usually `http://localhost:5173`).

Controls
- **Celebrate!** button for a big burst of confetti.
- Confetti flows by default on page load (no UI toggle).

Customization tips
- Change the target date in `src/main.js` (function `getNextBirthday`).
- Tweak confetti behavior in `src/confetti.js` (spawnRate, maxParticles).

Enjoy! 🎉# adi_23
